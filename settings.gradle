rootProject.name = 'explorer-api-common-parent'

include 'explorer-api-common'
project(':explorer-api-common').name = 'explorer-api-common-lib' // used to avoid conflict with parent directory name

include 'explorer-api-common-test'

